<!DOCTYPE html>
<html>
  <head>
    <title>ResumeGame</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link href="https://fonts.googleapis.com/css?family=Chakra+Petch" rel="stylesheet">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="navigation">
      <ul>
        <%# This nav bar should show on every page but the homepage/login page%>
        <% if @user %>
            Welcome <%= @user.fullname %>
            <a href="#">About</a>
            <a href="#">Profile</a>
            <a href="#">Analytics</a>
            <a href="/logout">Logout</a>
        <% end %>
      </ul>
    </div>
    <%= yield %>
  </body>
  <footer>
      <p>resume game? is a proof-of-concept project designed by Srikant Kalaputapu and Trevor Jameson</p>
  </footer>
</html>
